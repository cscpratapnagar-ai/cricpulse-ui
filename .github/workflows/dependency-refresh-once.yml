name: One-time formatting repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: echo "One-time formatting repair completed; normal CI remains verification-only."
