import { CommonModule } from '@angular/common';
import { HttpClient } from '@angular/common/http';
import { Component, inject } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';

interface FormMatch { matchId:string; matchName:string; date?:string|null; teamName:string; runs:number; wickets:number; balls?:number; economy?:number; result?:string; }
@Component({selector:'app-player-form',standalone:true,imports:[CommonModule,RouterLink],template:`
<section class="page">
@if(loading){<section class="card state">Loading player form…</section>}
@else if(playerName){
<a class="back" [routerLink]="['/players',playerId]">← Player Profile</a>
<header class="hero card"><div><span class="eyebrow">CRICPULSE · PLAYER INTELLIGENCE</span><h1>{{playerName}}</h1><p>Recent-match form and performance trends.</p></div><div class="form-badge" [class.down]="formScore<50">{{formLabel}}</div></header>
<section class="summary-grid"><article class="card stat"><span>FORM SCORE</span><strong>{{formScore}}</strong><small>Last {{matches.length}} recorded matches</small></article><article class="card stat"><span>RECENT RUNS</span><strong>{{recentRuns}}</strong><small>{{averageRuns}} runs / match</small></article><article class="card stat"><span>RECENT WICKETS</span><strong>{{recentWickets}}</strong><small>{{averageWickets}} wickets / match</small></article><article class="card stat"><span>BEST SCORE</span><strong>{{bestRuns}}</strong><small>Highest recent innings</small></article></section>
<section class="grid"><section class="card panel"><div class="head"><div><span class="eyebrow">BATTING FORM</span><h2>Last {{matches.length}} matches</h2></div></div>@if(matches.length){<div class="bars">@for(m of matches;track $index){<div class="bar-item"><div class="bar-value">{{m.runs}}</div><div class="bar" [style.height.px]="barHeight(m.runs)"></div><span>{{label($index)}}</span></div>}</div>}@else{<p class="muted">No recent match data available.</p>}</section>
<section class="card panel"><div class="head"><div><span class="eyebrow">BOWLING FORM</span><h2>Wickets trend</h2></div></div>@if(matches.length){<div class="bars">@for(m of matches;track $index){<div class="bar-item"><div class="bar-value">{{m.wickets}}</div><div class="bar wicket-bar" [style.height.px]="wicketHeight(m.wickets)"></div><span>{{label($index)}}</span></div>}</div>}@else{<p class="muted">No recent bowling data available.</p>}</section></section>
<section class="card panel table-card"><div class="head"><div><span class="eyebrow">RECENT PERFORMANCE</span><h2>Match-by-match form</h2></div></div><div class="table-wrap"><table><thead><tr><th>MATCH</th><th>TEAM</th><th>R</th><th>W</th><th>RESULT</th></tr></thead><tbody>@for(m of matches;track m.matchId){<tr><td><a [routerLink]="['/matches',m.matchId]">{{m.matchName}}</a><small>{{m.date || 'Completed'}}</small></td><td>{{m.teamName}}</td><td class="accent">{{m.runs}}</td><td>{{m.wickets}}</td><td>{{m.result || '—'}}</td></tr>}</tbody></table></div></section>
<section class="card insight"><span class="eyebrow">FORM INSIGHT</span><h2>{{insight}}</h2><p>Form score is a transparent recent-performance indicator based on batting output and wickets. It does not change official match statistics.</p></section>
} @else {<section class="card state"><h2>Player form unavailable</h2><a routerLink="/players/statistics">Back to rankings →</a></section>}
</section>`,styles:[`:host{display:block}.page{max-width:1180px;margin:auto;padding:34px 4vw 100px;color:#edf8f2}.card{border:1px solid #ffffff16;border-radius:22px;background:linear-gradient(180deg,#0f271ee8,#091712f7);box-shadow:0 18px 50px #0006}.back{display:inline-block;margin-bottom:18px;color:#91aa9d;text-decoration:none;font-size:12px}.back:hover{color:#b8f45c}.eyebrow{display:block;color:#789386;font-size:9px;font-weight:900;letter-spacing:2px}.hero{display:flex;justify-content:space-between;align-items:center;padding:30px}.hero h1{margin:8px 0 5px;font-size:clamp(36px,5vw,58px);letter-spacing:-3px}.hero p{margin:0;color:#91aa9d;font-size:11px}.form-badge{padding:13px 16px;border-radius:13px;background:#b8f45c;color:#10251e;font-size:12px;font-weight:950;letter-spacing:1px}.form-badge.down{background:#ff766d;color:#21100f}.summary-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:18px 0}.stat{padding:20px}.stat span{display:block;color:#789386;font-size:8px;font-weight:900;letter-spacing:1.5px}.stat strong{display:block;margin:10px 0 4px;font-size:37px;line-height:1;letter-spacing:-2px}.stat small{color:#789386;font-size:9px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.panel{padding:22px}.head{display:flex;justify-content:space-between;margin-bottom:18px}.head h2{margin:6px 0 0;font-size:18px}.bars{height:220px;display:flex;align-items:end;justify-content:space-around;gap:10px;padding:20px 5px 0;border-bottom:1px solid #ffffff14}.bar-item{height:100%;flex:1;display:flex;flex-direction:column;justify-content:end;align-items:center;min-width:25px}.bar-value{font-size:9px;color:#b8f45c;font-weight:900;margin-bottom:5px}.bar{width:min(34px,70%);height:20px;border-radius:8px 8px 2px 2px;background:#b8f45c}.wicket-bar{background:#edf8f2}.bar-item span{font-size:8px;color:#789386;margin-top:8px}.table-card{margin-top:18px;overflow:hidden}.table-wrap{overflow:auto}table{width:100%;min-width:650px;border-collapse:collapse;table-layout:fixed}th,td{padding:13px 16px;border-top:1px solid #ffffff0d;text-align:right;font-size:10px}th{color:#789386;font-size:8px;letter-spacing:1.4px;text-align:right}th:first-child,td:first-child{text-align:left;width:34%}th:nth-child(2),td:nth-child(2){text-align:left;width:22%}td a{color:#edf8f2;text-decoration:none;font-weight:900}td a:hover{color:#b8f45c}td small{display:block;color:#789386;font-size:8px;margin-top:3px}.accent{color:#c9ff71;font-weight:950}.insight{margin-top:18px;padding:22px}.insight h2{margin:7px 0 5px;font-size:18px}.insight p,.muted{margin:0;color:#789386;font-size:10px;line-height:1.6}.state{padding:50px}.state h2{margin-bottom:20px}.state a{padding:12px 16px;background:#b8f45c;color:#10251e;border-radius:10px;text-decoration:none;font-size:10px;font-weight:900}@media(max-width:800px){.summary-grid{grid-template-columns:1fr 1fr}.grid{grid-template-columns:1fr}.hero{align-items:flex-start;gap:18px}}@media(max-width:500px){.page{padding:24px 16px 70px}.hero{flex-direction:column}.summary-grid{grid-template-columns:1fr}.bars{height:190px}.form-badge{width:max-content}}`]})
export class PlayerFormComponent{
 private readonly http=inject(HttpClient);private readonly route=inject(ActivatedRoute);readonly api='http://localhost:8080/api';playerId=this.route.snapshot.paramMap.get('id')||'';loading=true;playerName='';matches:FormMatch[]=[];
 constructor(){if(this.playerId)this.load();else this.loading=false;}
 private load(){this.http.get<any>(`${this.api}/players/${this.playerId}`).subscribe({next:p=>{this.playerName=p?.name||'Player';this.loadMatches();},error:()=>this.loading=false});}
 private loadMatches(){this.http.get<FormMatch[]>(`${this.api}/players/${this.playerId}/recent-matches`).subscribe({next:r=>{this.matches=(r||[]).slice(0,10).reverse();this.loading=false;},error:()=>{this.matches=[];this.loading=false;}});}
 get recentRuns(){return this.matches.reduce((s,m)=>s+(m.runs||0),0);}get recentWickets(){return this.matches.reduce((s,m)=>s+(m.wickets||0),0);}get bestRuns(){return this.matches.reduce((x,m)=>Math.max(x,m.runs||0),0);}get averageRuns(){return this.matches.length?(this.recentRuns/this.matches.length).toFixed(1):'0.0';}get averageWickets(){return this.matches.length?(this.recentWickets/this.matches.length).toFixed(1):'0.0';}
 get formScore(){if(!this.matches.length)return 0;const max=this.matches.reduce((s,m)=>s+Math.min(m.runs||0,50),0);const wicketScore=this.recentWickets*10;return Math.min(100,Math.round((max/(this.matches.length*50))*75+(wicketScore/(this.matches.length*20))*25));}
 get formLabel(){return this.formScore>=75?'EXCELLENT':this.formScore>=55?'GOOD':this.formScore>=35?'AVERAGE':'NEEDS IMPROVEMENT';}
 get insight(){if(!this.matches.length)return 'Not enough recent data to assess form.';if(this.formScore>=75)return 'Excellent recent form — strong batting output and/or wicket contribution.';if(this.formScore>=55)return 'Good recent form with useful contributions across the latest matches.';if(this.formScore>=35)return 'Mixed recent form. More matches will make the trend more reliable.';return 'Recent output is below the current benchmark. Keep tracking the next matches.';}
 barHeight(v:number){const max=Math.max(1,...this.matches.map(x=>x.runs||0));return Math.max(10,Math.round(((v||0)/max)*145));}wicketHeight(v:number){const max=Math.max(1,...this.matches.map(x=>x.wickets||0));return Math.max(10,Math.round(((v||0)/max)*145));}label(i:number){return `M${i+1}`;}
}
