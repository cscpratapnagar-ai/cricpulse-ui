import { BroadcastControlComponent } from './broadcast-control.component';

describe('BroadcastControlComponent', () => {
  it('should expose the component class', () => expect(BroadcastControlComponent).toBeTruthy());
});
