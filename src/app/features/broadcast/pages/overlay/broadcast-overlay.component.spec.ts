import { BroadcastOverlayComponent } from './broadcast-overlay.component';

describe('BroadcastOverlayComponent', () => {
  it('should expose the component class', () => expect(BroadcastOverlayComponent).toBeTruthy());
});
