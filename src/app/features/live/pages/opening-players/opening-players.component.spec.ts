import { OpeningPlayersComponent } from './opening-players.component';
describe('OpeningPlayersComponent', () => {
  it('should expose the component class', () => expect(OpeningPlayersComponent).toBeTruthy());
});
