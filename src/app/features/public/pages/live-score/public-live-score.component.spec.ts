import { PublicLiveScoreComponent } from './public-live-score.component';
describe('PublicLiveScoreComponent',()=>{it('should expose the component class',()=>expect(PublicLiveScoreComponent).toBeTruthy());});
