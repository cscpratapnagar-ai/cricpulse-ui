import { AccountProfileComponent } from './account-profile.component';

describe('account-profile.component', () => {
  it('should expose the component class', () => {
    expect(AccountProfileComponent).toBeTruthy();
  });
});
