import { PlayerComparisonComponent } from './player-comparison.component';
describe('PlayerComparisonComponent',()=>{it('should expose the component class',()=>expect(PlayerComparisonComponent).toBeTruthy());});
