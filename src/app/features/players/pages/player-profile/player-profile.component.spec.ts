import { PlayerProfileComponent } from './player-profile.component';
describe('PlayerProfileComponent',()=>{it('should expose the component class',()=>expect(PlayerProfileComponent).toBeTruthy());});
