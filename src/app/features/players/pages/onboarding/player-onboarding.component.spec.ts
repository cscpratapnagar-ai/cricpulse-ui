import { PlayerOnboardingComponent } from './player-onboarding.component';
describe('PlayerOnboardingComponent',()=>{it('should expose the component class',()=>expect(PlayerOnboardingComponent).toBeTruthy());});
