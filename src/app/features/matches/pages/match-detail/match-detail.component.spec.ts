import { MatchDetailComponent } from './match-detail.component';
describe('MatchDetailComponent', () => {
  it('should expose the component class', () => expect(MatchDetailComponent).toBeTruthy());
});
