import { MatchStatisticsComponent } from './match-statistics.component';
describe('MatchStatisticsComponent',()=>{it('should expose the component class',()=>expect(MatchStatisticsComponent).toBeTruthy());});
