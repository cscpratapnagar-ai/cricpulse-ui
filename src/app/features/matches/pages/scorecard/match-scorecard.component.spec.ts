import { MatchScorecardComponent } from './match-scorecard.component';
describe('MatchScorecardComponent', () => {
  it('should expose the component class', () => expect(MatchScorecardComponent).toBeTruthy());
});
