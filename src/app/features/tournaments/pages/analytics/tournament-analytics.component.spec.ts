import { TournamentAnalyticsComponent } from './tournament-analytics.component';
describe('TournamentAnalyticsComponent',()=>{it('should expose the component class',()=>expect(TournamentAnalyticsComponent).toBeTruthy());});
