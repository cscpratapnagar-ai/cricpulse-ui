import { TournamentDetailComponent } from './tournament-detail.component';
describe('TournamentDetailComponent', () => {
  it('should expose the component class', () => expect(TournamentDetailComponent).toBeTruthy());
});
