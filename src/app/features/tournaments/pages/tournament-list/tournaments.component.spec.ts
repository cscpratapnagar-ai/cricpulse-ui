import { TournamentsComponent } from './tournaments.component';
describe('TournamentsComponent', () => {
  it('should expose the component class', () => expect(TournamentsComponent).toBeTruthy());
});
