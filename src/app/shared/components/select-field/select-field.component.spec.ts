import { SelectFieldComponent } from './select-field.component';
describe('SelectFieldComponent', () => {
  it('should expose the component class', () => expect(SelectFieldComponent).toBeTruthy());
});
