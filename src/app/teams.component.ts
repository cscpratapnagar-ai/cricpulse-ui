import { HttpClient } from '@angular/common/http';
import { Component, inject } from '@angular/core';
import { StateViewComponent } from './state-view.component';
import { Router, RouterLink } from '@angular/router';

interface Team { id: string; name: string; city?: string; ownerId: string; }

@Component({selector:'app-teams',standalone:true,imports:[RouterLink,StateViewComponent],template:`<section class="teams-page"><div class="page-head"><div><div class="eyebrow">Team management</div><h1>My Teams</h1><p>Manage every team you own from one place.</p></div><button class="primary" routerLink="/dashboard/teams/new">+ Create Team</button></div>@if(loading){<app-state-view state="loading" loadingLabel="Loading your teams..."></app-state-view>}@else if(error){<app-state-view state="error" title="Unable to load teams" message="We couldn't retrieve your teams. Check the connection and try again." (retry)="loadTeams()"></app-state-view>}@else if(teams.length){<div class="team-grid">@for(t of teams;track t.id){<article class="team-card" [class.active]="t.id===activeTeamId"><div class="team-top"><div class="team-logo">{{t.name.charAt(0).toUpperCase()}}</div><span class="badge">{{t.id===activeTeamId?'ACTIVE':'OWNER'}}</span></div><h2>{{t.name}}</h2><p>◉ {{t.city||'Location not set'}}</p><div class="team-actions"><button type="button" class="open" (click)="openTeam(t)">Open Team →</button><span>Created by you</span></div></article>}</div>@if(activeTeam){<section class="active-preview"><div><div class="eyebrow">ACTIVE WORKSPACE</div><h2>{{activeTeam.name}}</h2><p>◉ {{activeTeam.city||'Location not set'}} · Created by you</p></div><button type="button" class="primary" (click)="openTeam(activeTeam)">Manage Team →</button></section>}}@else{<app-state-view state="empty" title="No teams yet" message="Create your first team to start building your squad." actionLabel="Create Team" (action)="createTeam()"></app-state-view>}</section>`,styles:[`:host{display:block}.teams-page{max-width:1180px;margin:0 auto;padding:42px clamp(18px,4vw,52px) 90px}.page-head{display:flex;justify-content:space-between;align-items:flex-end;gap:24px;margin-bottom:30px}.eyebrow{color:var(--cp-accent);font-size:9px;font-weight:950;letter-spacing:.16em;text-transform:uppercase}h1{margin:9px 0 8px;color:var(--cp-text);font-size:clamp(34px,5vw,54px);line-height:1;letter-spacing:-.055em}h2{color:var(--cp-text)}p{color:var(--cp-text-muted)}.primary,.open{display:inline-flex;align-items:center;justify-content:center;gap:9px;border:1px solid transparent;border-radius:11px;background:var(--cp-accent);color:var(--cp-accent-contrast);font:900 10px inherit;letter-spacing:.01em;text-decoration:none;cursor:pointer;transition:transform .18s,box-shadow .18s}.primary{min-height:42px;padding:0 16px;box-shadow:0 10px 24px var(--cp-accent-soft)}.primary:hover,.open:hover{transform:translateY(-1px);box-shadow:0 14px 28px var(--cp-accent-soft)}.team-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:15px}.team-card{position:relative;overflow:hidden;padding:20px;border:1px solid var(--cp-border);border-radius:20px;background:linear-gradient(145deg,var(--cp-surface),var(--cp-surface-raised));box-shadow:var(--cp-shadow-sm);transition:transform .2s,border-color .2s,box-shadow .2s}.team-card:before{content:'';position:absolute;inset:0 0 auto;height:2px;background:var(--cp-accent);opacity:0;transition:.2s}.team-card:hover{border-color:color-mix(in srgb,var(--cp-accent) 45%,var(--cp-border));transform:translateY(-3px);box-shadow:var(--cp-shadow-lg)}.team-card.active{border-color:color-mix(in srgb,var(--cp-accent) 58%,var(--cp-border));background:linear-gradient(145deg,color-mix(in srgb,var(--cp-accent-soft) 40%,var(--cp-surface)),var(--cp-surface))}.team-card.active:before{opacity:1}.team-top{display:flex;justify-content:space-between;align-items:center}.team-logo{display:grid;place-items:center;width:54px;height:54px;border-radius:16px;background:var(--cp-accent);color:var(--cp-accent-contrast);font-size:22px;font-weight:950;box-shadow:0 10px 24px var(--cp-accent-soft)}.badge{font-size:8px;font-weight:950;letter-spacing:.12em;color:var(--cp-accent);border:1px solid color-mix(in srgb,var(--cp-accent) 35%,var(--cp-border));padding:6px 8px;border-radius:999px;background:var(--cp-accent-soft)}.team-card h2{margin:18px 0 7px;font-size:20px;letter-spacing:-.025em}.team-card>p{display:flex;align-items:center;gap:6px;margin:0;font-size:10px}.team-actions{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:22px;padding-top:14px;border-top:1px solid var(--cp-border)}.team-actions span{font-size:9px;color:var(--cp-text-muted)}.open{padding:9px 11px}.active-preview{position:relative;display:flex;justify-content:space-between;align-items:center;gap:20px;margin-top:22px;padding:24px 26px;border:1px solid color-mix(in srgb,var(--cp-accent) 32%,var(--cp-border));border-radius:20px;background:linear-gradient(120deg,var(--cp-accent-soft),var(--cp-surface));overflow:hidden}.active-preview:after{content:'ACTIVE';position:absolute;right:-8px;bottom:-22px;color:var(--cp-accent);font-size:62px;font-weight:950;letter-spacing:-.08em;opacity:.055;pointer-events:none}.active-preview h2{margin:8px 0 6px;font-size:26px;letter-spacing:-.035em}.active-preview p{margin:0;font-size:10px}.active-preview .primary{position:relative;z-index:1;white-space:nowrap}@media(max-width:900px){.team-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:620px){.teams-page{padding:30px 16px 72px}.page-head,.active-preview{align-items:stretch;flex-direction:column}.team-grid{grid-template-columns:1fr}.primary{width:100%}.team-actions{align-items:flex-start;flex-direction:column}.active-preview .primary{width:100%}}`]})
export class TeamsComponent { private readonly http=inject(HttpClient); private readonly router=inject(Router); teams:Team[]=[]; activeTeam:Team|null=null; activeTeamId:string|null=null; loading=true; error=false; constructor(){this.loadTeams();} loadTeams(){this.loading=true;this.error=false;this.http.get<Team[]>('http://localhost:8080/api/teams/mine').subscribe({next:teams=>{this.teams=teams;const saved=localStorage.getItem('cricketpulse_active_team_id');this.activeTeam=teams.find(t=>t.id===saved)||teams[0]||null;this.activeTeamId=this.activeTeam?.id||null;this.loading=false;},error:()=>{this.teams=[];this.activeTeam=null;this.activeTeamId=null;this.error=true;this.loading=false;}})} createTeam(){this.router.navigate(['/dashboard/teams/new']);} selectTeam(t:Team){this.activeTeam=t;this.activeTeamId=t.id;localStorage.setItem('cricketpulse_active_team_id',t.id);} openTeam(t:Team){this.selectTeam(t);this.router.navigate(['/dashboard/teams',t.id]);}}
